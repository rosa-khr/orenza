---
import "../styles/global.css";
import "../styles/luxury.css";
import "../styles/reference.css";
import "../styles/minimal-luxury.css";
import CartDrawer from "../components/CartDrawer.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";

interface Props {
  title: string;
  description: string;
  canonicalPath: string;
  keywords: string[];
  pageType?: "website" | "article";
  noindex?: boolean;
}

const {
  title,
  description,
  canonicalPath,
  keywords,
  pageType = "website",
  noindex = false
} = Astro.props;

const siteUrl = Astro.site ?? new URL(Astro.url.origin);
const canonical = new URL(canonicalPath, siteUrl);
const socialImage = new URL("/images/orenza-leopard-label.png", siteUrl);
const fullTitle = `${title} | اورنزا`;

const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      "@id": `${siteUrl}#organization`,
      name: "Orenza Coffee",
      alternateName: "قهوه اورنزا",
      url: siteUrl.toString(),
      logo: socialImage.toString(),
      email: "order.orenzacoffee@gmail.com",
      telephone: "+989103060396",
      contactPoint: {
        "@type": "ContactPoint",
        telephone: "+989103060396",
        contactType: "customer service",
        availableLanguage: ["fa"]
      },
      sameAs: ["https://instagram.com/orenzacoffee", "https://ble.ir/khoobrooz"]
    },
    {
      "@type": "WebSite",
      "@id": `${siteUrl}#website`,
      name: "قهوه اورنزا",
      alternateName: "Orenza Coffee",
      url: siteUrl.toString(),
      inLanguage: "fa-IR",
      publisher: { "@id": `${siteUrl}#organization` }
    },
    {
      "@type": "WebPage",
      "@id": `${canonical}#webpage`,
      name: fullTitle,
      description,
      url: canonical.toString(),
      inLanguage: "fa-IR",
      isPartOf: { "@id": `${siteUrl}#website` },
      about: { "@id": `${siteUrl}#organization` }
    }
  ]
};
---

<!doctype html>
<html lang="fa" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#0b0b0a" />
    <meta name="description" content={description} data-site-meta="description" />
    <meta name="keywords" content={keywords.join(", ")} data-site-meta="keywords" />
    <meta name="robots" content={noindex ? "noindex, nofollow" : "index, follow, max-image-preview:large"} data-site-meta="robots" />
    <meta name="author" content="Orenza Coffee" />
    <meta property="og:type" content={pageType} />
    <meta property="og:locale" content="fa_IR" />
    <meta property="og:site_name" content="Orenza Coffee" />
    <meta property="og:title" content={fullTitle} data-site-meta="og:title" />
    <meta property="og:description" content={description} data-site-meta="og:description" />
    <meta property="og:url" content={canonical.toString()} />
    <meta property="og:image" content={socialImage.toString()} data-site-meta="og:image" />
    <meta property="og:image:alt" content="نشان تصویری پلنگ ایرانی اورنزا" />
    <meta property="og:image:width" content="620" />
    <meta property="og:image:height" content="895" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} data-site-meta="twitter:title" />
    <meta name="twitter:description" content={description} data-site-meta="twitter:description" />
    <meta name="twitter:image" content={socialImage.toString()} data-site-meta="twitter:image" />
    <link rel="canonical" href={canonical.toString()} />
    <link rel="alternate" hreflang="fa-IR" href={canonical.toString()} />
    <link rel="alternate" hreflang="x-default" href={canonical.toString()} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" data-site-favicon />
    {!noindex && <script type="application/ld+json" is:inline set:html={JSON.stringify(schema)}></script>}
    <title>{fullTitle}</title>
  </head>
  <body>
    <Header />
    <main id="top"><slot /></main>
    <Footer />
    <CartDrawer />
    <script>
      import "../scripts/configurator.ts";
    </script>
    <script>
      import "../scripts/site-settings.ts";
    </script>
    <script>
      const key = "orenza-visitor-id";
      let visitorId = localStorage.getItem(key);
      if (!visitorId) {
        visitorId = crypto.randomUUID();
        localStorage.setItem(key, visitorId);
      }
      void fetch("/api/v1/analytics/visit", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ visitorId, path: location.pathname }),
        keepalive: true
      });
    </script>
  </body>
</html>
